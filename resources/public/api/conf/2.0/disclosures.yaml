get:
  $ref: "./retrieve.yaml"

  summary: Retrieve Disclosures [test only]

  security:
    - User-Restricted:
        - read:self-assessment

put:
  $ref: "./amend.yaml"

  summary: Create and Amend Disclosures [test only]

  security:
    - User-Restricted:
        - write:self-assessment

delete:
  $ref: "./delete.yaml"

  summary: Delete Disclosures [test only]

  security:
    - User-Restricted:
        - write:self-assessment
