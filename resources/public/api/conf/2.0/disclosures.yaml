get:
  $ref: "./retrieve.yaml"

  security:
    - User-Restricted:
        - read:self-assessment

put:
  $ref: "./amend.yaml"

  security:
    - User-Restricted:
        - write:self-assessment

delete:
  $ref: "./delete.yaml"

  security:
    - User-Restricted:
        - write:self-assessment
